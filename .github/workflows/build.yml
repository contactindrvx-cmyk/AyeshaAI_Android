# --- عائشہ AI اینڈرائیڈ APK بلڈ ورک فلو (FINAL STABLE VERSION) ---
name: Build Ayesha AI APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python_version: '3.10'

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Install Build Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y git zip unzip python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev ant openjdk-17-jdk
        pip install --upgrade pip
        # Cython اور Buildozer کا ہونا ضروری ہے
        pip install buildozer kivy cython

    - name: Build with Buildozer
      run: |
        # 'yes |' ڈالنے سے یہ لائسنس کے تمام سوالات خود حل کر دے گا
        yes | buildozer android debug
      env:
        ANDROID_SDK_LICENSE_AGREED: 'yes'
        ACCEPT_LICENSES: 'yes'

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Ayesha_AI_Debug_APK
        path: bin/*.apk
        
